# Pure-math analysis stack (metrics, stats, pooling, experiments driver).
# These are all that's needed to run the test suite and the analysis on
# already-extracted embeddings.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0

# Model-dependent extraction (extraction.py, baselines.py, validation.py).
# Only needed when tapping a live TRIBE v2 checkpoint; not required for tests.
#   torch>=2.2
#   einops>=0.7
#   tribev2  @ git+https://github.com/facebookresearch/tribev2
